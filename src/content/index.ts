import original from "./original";
import maya from "./maya";

/**
 * Switch the active site with ONE line:
 *  - `original` → step 1: faithful homepage clone (assignment demo only)
 *  - `maya`     → step 2: redesigned site for Dr. Maya Reynolds (final deliverable)
 */
export const site = maya; // ← change to `original` to view the clone

export { original, maya };